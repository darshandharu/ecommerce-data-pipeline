"""Reusable, composable DataFrame transformations.

These functions are deliberately pure (DataFrame -> DataFrame) so they can be
unit-tested in isolation and chained via ``DataFrame.transform``.
"""
