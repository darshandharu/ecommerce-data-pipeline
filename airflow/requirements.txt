# Extra libs layered on top of the apache/airflow base image.
#
# Kept intentionally lean: the local DAGs run the PySpark jobs via BashOperator,
# so the workers need PySpark + PyYAML (config loading) + pandas. dbt and the
# cloud providers are NOT installed here to avoid pip dependency conflicts with
# the Airflow base image; in production those run in their own images / Dataproc
# (see docs/deployment.md). Locally, the dbt tasks are expected to fail without
# a BigQuery target.
pyspark==3.5.1
pyyaml==6.0.1
pandas==2.2.2
